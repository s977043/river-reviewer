# a11y Accessible Name Basics — eval scaffolding

Status: **fixtures + eval scaffolding only**. `golden/` is intentionally empty.

See the companion `rr-midstream-modern-web-semantic-001/README.md` for the
rationale (we don't hand-write goldens) and the workflow to generate them.

Promotion to `recommended: true` in `skills/registry.yaml` requires verified
golden output for each fixture + promptfoo eval in CI.
