name: 'River Review (example-2: upstream only)'
on:
  pull_request:

jobs:
  river-review:
    runs-on: ubuntu-latest
    permissions:
      contents: read
      pull-requests: write
      issues: write
    steps:
      - uses: actions/checkout@v6
        with:
          fetch-depth: 0
      - name: Run River Review (upstream)
        uses: s977043/river-review/runners/github-action@v0.1.1
        with:
          phase: upstream
          dry_run: true
