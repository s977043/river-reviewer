[
  {
    "name": "i18n: deleted source reference leaves unused locale entry",
    "phase": "midstream",
    "category": "i18n",
    "seedRepo": "seeds/i18n-unused-key-01/",
    "diffFile": "diffs/i18n-unused-key-01.diff",
    "planSkills": ["rr-midstream-i18n-unused-key-001"],
    "expected": {
      "withRepoContext": {
        "minFindings": 0,
        "note": "PR-1 starter: assertions are loose; subsequent PRs (#688 PR-2..) tighten detection bands as cross-context skills mature."
      },
      "withoutRepoContext": {
        "minFindings": 0,
        "note": "Without repo context the locale JSON is invisible; the diff alone cannot reveal an unused entry."
      }
    }
  },
  {
    "name": "normalization: new caller bypasses formatId helper",
    "phase": "midstream",
    "category": "normalization",
    "seedRepo": "seeds/normalization-id-format-01/",
    "diffFile": "diffs/normalization-id-format-01.diff",
    "planSkills": ["rr-midstream-normalization-consistency-001"],
    "expected": {
      "withRepoContext": {
        "minFindings": 0,
        "note": "Should-detect: the seed exposes formatId.ts and an existing fetchAccount caller; with repo context the new direct call site looks suspicious."
      },
      "withoutRepoContext": {
        "minFindings": 0,
        "note": "Diff alone shows a fresh component file with no obvious red flag; expect lower detection without context."
      }
    }
  },
  {
    "name": "loading: removed loading guard leaves null deref on initial render",
    "phase": "midstream",
    "category": "loading",
    "seedRepo": "seeds/loading-state-early-return-01/",
    "diffFile": "diffs/loading-state-early-return-01.diff",
    "planSkills": ["rr-midstream-loading-state-001"],
    "expected": {
      "withRepoContext": {
        "minFindings": 0,
        "note": "Should-detect: useUser hook returns user=null while loading; removing the guard introduces a runtime null deref."
      },
      "withoutRepoContext": {
        "minFindings": 0,
        "note": "Without the hook contract visible the guard removal looks like a refactor."
      }
    }
  },
  {
    "name": "nullability: dereferences nullable discount field",
    "phase": "midstream",
    "category": "nullability",
    "seedRepo": "seeds/nullability-api-response-01/",
    "diffFile": "diffs/nullability-api-response-01.diff",
    "planSkills": ["rr-midstream-nullability-contract-001"],
    "expected": {
      "withRepoContext": {
        "minFindings": 0,
        "note": "Should-detect: Invoice.discount is `{ amount, reason } | null` per the API contract; the new component reads .amount unconditionally."
      },
      "withoutRepoContext": {
        "minFindings": 0,
        "note": "Diff alone shows a property access without the type definition to compare against."
      }
    }
  },
  {
    "name": "api-compat: new required field, existing test not updated",
    "phase": "midstream",
    "category": "api-compat",
    "seedRepo": "seeds/api-contract-no-test-01/",
    "diffFile": "diffs/api-contract-no-test-01.diff",
    "planSkills": ["rr-midstream-api-compatibility-001"],
    "expected": {
      "withRepoContext": {
        "minFindings": 0,
        "note": "Should-detect: tests/api/payment.test.ts still uses the old shape; the change makes idempotencyKey required without updating tests."
      },
      "withoutRepoContext": {
        "minFindings": 0,
        "note": "Diff alone shows an interface bump; the missing test update only surfaces with the test file in context."
      }
    }
  },
  {
    "name": "guard: stale TODO replaced with precise rationale (no risk)",
    "phase": "midstream",
    "category": "guard",
    "guard": true,
    "seedRepo": "seeds/guard-future-use-comment/",
    "diffFile": "diffs/guard-future-use-comment.diff",
    "planSkills": ["rr-midstream-i18n-unused-key-001"],
    "expected": {
      "withRepoContext": {
        "minFindings": 0,
        "note": "Guard: the diff only improves a comment's rationale; a finding here would be a false positive."
      },
      "withoutRepoContext": {
        "minFindings": 0,
        "note": "Same: pure-comment edits should not generate findings."
      }
    }
  },
  {
    "name": "guard: header update on a clearly-generated file",
    "phase": "midstream",
    "category": "guard",
    "guard": true,
    "seedRepo": "seeds/guard-generated-file/",
    "diffFile": "diffs/guard-generated-file.diff",
    "planSkills": ["rr-midstream-api-compatibility-001"],
    "expected": {
      "withRepoContext": {
        "minFindings": 0,
        "note": "Guard: the file is auto-generated (DO NOT EDIT MANUALLY header); review should not generate findings on the generator's bookkeeping comment."
      },
      "withoutRepoContext": {
        "minFindings": 0,
        "note": "Same."
      }
    }
  },
  {
    "name": "guard: source change with companion test updated in the same diff",
    "phase": "midstream",
    "category": "guard",
    "guard": true,
    "seedRepo": "seeds/guard-related-test-updated/",
    "diffFile": "diffs/guard-related-test-updated.diff",
    "planSkills": ["rr-midstream-api-compatibility-001"],
    "expected": {
      "withRepoContext": {
        "minFindings": 0,
        "note": "Guard: sum.ts and sum.test.ts both move together. A 'missing test update' finding would be a false positive."
      },
      "withoutRepoContext": {
        "minFindings": 0,
        "note": "Same: the test update is visible directly in the diff."
      }
    }
  }
]
